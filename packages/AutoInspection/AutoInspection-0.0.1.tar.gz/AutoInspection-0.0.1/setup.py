from setuptools import setup, find_packages
from inspection import __version__ as version

setup(
    name='AutoInspection',
    version=version,
    packages=find_packages(),
    install_requires=[],
    url='https://github.com/hexs/Auto-Inspection-AI',
)
