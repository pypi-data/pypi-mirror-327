[build-system]
requires = ["setuptools>=64", "setuptools_scm>=8"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages]
find = {}

[tool.setuptools_scm]

[project]
dynamic = ["version"]
name = "dkist-processing-visp"
requires-python = ">=3.11"
description = "Science processing code for the ViSP instrument on DKIST"
readme = "README.rst"
authors = [ {name = "NSO / AURA", email = "dkistdc@nso.edu"} ]
license = {text = "BSD-3-Clause"}
classifiers = [
  "Programming Language :: Python",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.11",
]
dependencies = [
  "dkist-processing-common == 10.5.12",
  "dkist-processing-math == 2.1.2",
  "dkist-processing-pac == 3.1.1",
  "dkist-header-validator == 5.1.1",
  "dkist-fits-specifications == 4.11.0",
  "dkist-service-configuration == 2.2",
  "dkist-spectral-lines == 3.0.0",
  "astropy == 6.1.5",
  "numpy == 1.26.4",
  "sunpy == 5.1.3",
  "scipy == 1.15.1",
  "scikit-image == 0.23.2",
  "scikit-learn == 1.5.0",
  "peakutils == 1.3.5",
  "Pillow == 10.3.0",
  "moviepy == 2.0.0",
]

[project.urls]
Homepage = "https://nso.edu/dkist/data-center/"
Repository = "https://bitbucket.org/dkistdc/dkist-processing-visp"
Documentation = "https://docs.dkist.nso.edu/projects/visp"
Help = "https://nso.atlassian.net/servicedesk/customer/portal/5"

[project.optional-dependencies]
test = [
  "pytest",
  "pytest-cov",
  "pytest-xdist",
  "pytest-mock",
  "hypothesis",
  "towncrier",
  "dkist-data-simulator>=5.2.3",
  "pydantic",
  "dkist-processing-visp[inventory]",
  "dkist-processing-visp[asdf]",
  "dkist-processing-visp[quality]",
]
inventory = [
  "dkist-processing-common[inventory]",
  "dkist-inventory==1.6.1",
]
asdf = [
  "dkist-processing-common[asdf]",
  "dkist-inventory[asdf]==1.6.1",
]
quality = [
  "dkist-quality==1.2.0",
]
grogu = [
  "dkist",
  "pyparsing",
  "dkist-inventory>=1.6.0",
  "dkist-processing-visp[test]",
]
docs = [
  "sphinx",
  "sphinx-astropy",
  "sphinx-changelog",
  "sphinx-autoapi == 3.3.3",
  "pytest",
  "towncrier",
  "dkist-sphinx-theme",
  "packaging",
]

[tool.pytest.ini_options]
markers = [
  "development: For tests that can only be run while developing with a sidecar proxy (as opposed to in bitbucket pipelines)"
]

[tool.coverage.run]
omit = [
    "dkist_processing_visp/tests/*",
    "dkist_processing_visp/__init__.py",
]

[tool.coverage.report]
show_missing = true

[tool.towncrier]
    package = "dkist_processing_visp"
    filename = "CHANGELOG.rst"
    directory = "changelog/"
    issue_format = "`#{issue} <https://bitbucket.org/dkistdc/dkist-processing-visp/pull-requests/{issue}>`__"
    title_format = "{version} ({project_date})"
    ignore = [".gitempty", "*.science.rst"]

[[tool.towncrier.type]]
      directory = "feature"
      name = "Features"
      showcontent = true

[[tool.towncrier.type]]
      directory = "bugfix"
      name = "Bugfixes"
      showcontent = true

[[tool.towncrier.type]]
      directory = "removal"
      name = "Removals"
      showcontent = true

[[tool.towncrier.type]]
      directory = "misc"
      name = "Misc"
      showcontent = true

[[tool.towncrier.type]]
      directory = "doc"
      name = "Documentation"
      showcontent = true
